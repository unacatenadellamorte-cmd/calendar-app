<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                ディープリンク(ARCHITECTURE-SPINE Epic5 AD-16 / AD-18)。
                スキーム calendar-app:// は jp.ryo.calendarapp から導出した暫定値。
                calendar-app://event/{id} と calendar-app://day/{date} の2形式を受け付ける
                (ホスト部は限定しない。形式の解釈は src/app/DeepLinkListener.tsx が行う)。
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="calendar-app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
            ホーム画面ウィジェット(ARCHITECTURE-SPINE Epic5 AD-12、Story 5.6)。
            `android.appwidget.action.APPWIDGET_UPDATE` は保護ブロードキャスト(システムのみが
            送信可能)なので `exported="false"` のまま OS からの更新を受け取れる。
        -->
        <receiver
            android:name=".widget.FeaturedEventsWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/featured_events_widget_info" />
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        端末カレンダー(ARCHITECTURE-SPINE Epic5 AD-13、Story 5.2)。
        読み取り専用スコープのみ。WRITE_CALENDAR は意図的に追加しない
        (誤って書き込み API を呼んでも OS が拒否するようにするため)。
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
</manifest>

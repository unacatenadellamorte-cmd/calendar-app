{
  "name": "@calendar-app/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "純粋ドメイン: selection / priority / pay-calc。何も import しない。フロントと Edge Function が共有する。",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}

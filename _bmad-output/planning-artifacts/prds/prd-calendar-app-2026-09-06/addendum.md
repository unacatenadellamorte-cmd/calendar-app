---
title: "カレンダーアプリ(仮) PRD 付録"
status: final
created: 2026-09-06
updated: 2026-09-12
---

# 付録 — PRD に載せない技術・検討メモ

PRD 本文は「能力(何ができるか)」に絞っている。ここには実装寄りの検討、代替案、後続工程(アーキテクチャ / UX)が必要とする材料を置く。

## 技術スタックの第一候補([ASSUMPTION] ── ユーザー回答は「おまかせ」)

| 層 | 第一候補 | 理由 | 実装結果 |
|---|---|---|---|
| フロント | ~~Next.js~~ → **Vite + React 19 + TypeScript + Tailwind CSS 4** | 当初は human-book(Next.js)踏襲を想定していたが、2026-09-06 のアーキテクチャ工程で Vite SPA に変更(static export の罠回避) | **実装・実機受け入れ完了(2026-09-11)** |
| スマホ化 | Capacitor で Web をラップ | React Native 書き直し不要。CRUD 中心アプリに RN の重さは不要(human-book の判断と同じ) | **未着手(Epic 5)**。human-book も Capacitor 実機テスト未実施 ── 前例が無いので、ウィジェット/通知/端末カレンダーの各プラグインはアーキテクチャ工程で個別に調査が要る |
| データ保管 | Supabase(Postgres + 認証 + RLS) | 複数端末同期・配布・OAuth トークン保管・将来のウィジェット連携に適合。human-book で実績 | **実装・実機受け入れ完了** |
| 外部接続 | Google Calendar API(OAuth 2.0、`calendar.readonly` 相当の最小スコープ) | v1 は読み取り専用のため書き込みスコープは要求しない | **実装・実機受け入れ完了** |

**アーキテクチャ工程で確定すること(Epic 5):**
- ~~データ保管を Supabase にするか端末内完結か~~ → 解決済み(Supabase)。
- Capacitor でホーム画面ウィジェット(iOS WidgetKit / Android App Widget)をどう提供するか。JS ブリッジだけでは native UI を書けないため、プラットフォームごとのネイティブコード or 既存プラグインの選定が要る。
- 端末カレンダー読み取りのプラグイン(`@capacitor-community/calendar` 等)の権限モデル・対応 OS バージョン。
- ローカル通知(`@capacitor/local-notifications` 等)のスケジューリング精度・端末再起動後の再登録・OS のバッテリー最適化による抑制。
- iOS は Apple Developer Program(年額)+ App Store 審査が要る。Android は開発者登録が無料・審査も軽い(PRD §9-8)。

## 代替案の検討メモ

### スマホ化の方式
- **Capacitor(採用候補)** — Web 資産再利用。ウィジェット・通知・端末カレンダーはネイティブプラグイン or ブリッジで対応。
- **Flutter / React Native で書き直し** — ウィジェットやウォッチ対応はネイティブに寄せやすいが、Web 先行方針と二重開発になる。却下寄り。
- **PWA のみでスマホ対応** — ホーム画面ウィジェット(iOS/Android のネイティブウィジェット)は PWA からは提供できない。コアの動機を満たせないため不十分。

### データ保管
- **Supabase(クラウド)** — 複数端末で同じカレンダー・優先度・シフトを見られる。配布時に各ユーザーのデータを分離(RLS)。サーバー依存。
- **端末内完結** — サーバー不要・プライバシー上シンプル。ただし複数端末で優先度やシフトが同期しない。Google OAuth のトークンをクライアントだけで安全に扱う設計難度が上がる。
- 中間案: ローカルファースト + 任意でクラウド同期。実装コスト高。

### 選抜ロジック(FR-9)の代替
- **優先度 → 開始時刻が早い順(採用候補)** — 予測しやすい。「次に何がある」を素直に返す。
- **優先度 → 今から近い順** — 進行中〜直近を強調。数分後に迫った予定を見逃しにくいが、開始済みの長い予定の扱いが要検討。
- **重なり枠は複数表示** — 枠を消費するため「1件だけ大きく」というコンパクトビューの狙いと相性が悪い。

## 競合(シフカレ)の機能要点 ── 給料計算 v2 の設計材料

web 調査(2026-09-06)。v1 では扱わないが、v2 の給料計算を設計するときのチェックリスト:

- お気に入りシフト(テンプレ)/ 曜日パターンでの繰り返し登録
- 時給 / 日給の選択、休日・深夜 / 早朝の割増、締め日、給料日、時給発生単位
- 勤務先(バイト先)別の集計、月別 / 年別の集計
- ホーム画面ウィジェット(複数レイアウト)

出典:
- https://apps.apple.com/jp/app/id903825183 (シフト給料計算カレンダー)
- https://apps.apple.com/jp/app/id747364886 (シフトボード)
- https://app-liv.jp/lifestyle/scheduler/1413/ (2026年 シフト管理アプリ比較)

## UX 工程への申し送り

- コアの価値(優先度が表示に効く)が最も伝わるのは **コンパクトビュー / ウィジェット**。UX はここを主役に据える。
- **優先度の設定 UI** はドラッグでの並べ替えを想定(FR-6)。カレンダー一覧画面がそのまま優先度設定画面を兼ねる。
- **ワンタップ・シフト入力**(FR-12)の操作数が SM-3(3タップ以内)を満たすフローを設計する。
- v1 は Web / スマホブラウザ。狭い画面での月・週ビューの優先表示(FR-7)の見せ方が難所。
- **Epic 5 追記(2026-09-12):** ウィジェット(FR-18)は DESIGN.md のトークンをそのままネイティブ UI に落とし込む(色・タイポグラフィは既存のコンパクトビューと統一)。リマインダー設定(FR-20)は予定詳細シートに「リマインダーを追加」の導線を1つ足す程度に留め、既存の EXPERIENCE.md Voice(静かなトーン、感嘆符なし)を踏襲する。

## 論点の状態(ブリーフからの引き継ぎ + PRD で更新)

| 論点 | 状態 |
|---|---|
| 優先度の作用 | **確定・実装完了**: 並び順 + 代表予定の選抜(両方)。FR-6〜FR-10 |
| 同期の方向 | **確定・実装完了**: v1 読み取り専用。書き戻しは v2。FR-4 Out of Scope |
| 給料計算 v1 の範囲 | **確定・実装完了**: 時給 ×(終了−開始−休憩)の暦月合計。割増・締め日・勤務先別は v2。FR-13, FR-14 |
| 代表予定の選抜ロジック詳細 | **確定・実装完了**: 優先度 → 開始時刻が早い順、終日は後。PRD §9-1 |
| ウィジェット / 端末カレンダー / 通知の範囲 | **2026-09-12 更新**: v1(Web)は無し。**Epic 5** でこの3つを追加(PRD §4.6)。ウォッチのみ引き続き対象外(v2+)。通知は当初想定の「選抜結果ダイジェスト」ではなく予定ごとの個別リマインダーに変更。§8 |
| Epic 5 の対象プラットフォーム | **2026-09-12 確定**: Android・iOS 両方同時。iOS は Apple Developer Program + 審査が要ることは把握済み(PRD §9-8) |
| データ保管 / スタック | **確定・実装完了**: Supabase + Vite(Next.js から変更)。上表参照 |
| 配布 / 課金 | 配布はする(無料)。課金モデル未定。PRD §9-6 |
| プロダクト名 | 未定 |

# Epic 1 / 2 / 4 受け入れ検証(Phase C 方式)

日付: 2026-09-11 · 検証者: Claude(実機) · アプリ: localhost:5173(dev)· 承認方針: Ryo が「Phase C 方式で AC を1つずつ系統的に」を選択

足場(2026-09-11 確認):
- `npm run typecheck` clean / `npm run lint` clean / `npm test` = 435 passed(64 files)/ `npm run build` green(chunk-size 警告のみ、既知)

判定記号: ✅ 合致 / ⚠️ 逸脱(容認・記録)/ ❌ 不合致(要修正)/ 🖥️ 実機外(この環境で実行時検証不可、代替根拠を記す)

---

## Epic 1 — 自分の予定をカレンダーで管理する(+ 基盤)

### Story 1.1 プロジェクト基盤とアプリシェル
- [ ] AC1 構成でビルドが通る / `supabase start` が起動
- [ ] AC2 下タブ3画面、ドロワー/ハンバーガーなし
- [ ] AC3 テーマトークン(light/dark)が CSS 変数 + Tailwind で両テーマ適用

### Story 1.2 メール+パスワードのアカウントとお試しモード
- [ ] AC1 サインアップ→ログイン→ログアウトでセッション確立・破棄、全テーブル RLS
- [ ] AC2 未ログインでもホーム・カレンダー表示、ローカル CRUD 可
- [ ] AC3 未ログインで Google 接続 → ログイン誘導

### Story 1.3 カレンダーの作成・管理
- [ ] AC1 作成・改名・色変更・削除が `calendars` + RLS に反映、一覧に source 明示
- [ ] AC2 「シフト用カレンダー」既定1つ、改名・色可・削除不可
- [ ] AC3 初回(カレンダー0)で「接続」「予定を追加」2入口

### Story 1.4 ローカル予定の作成・編集・削除
- [ ] AC1 予定 CRUD が `events`(timestamptz/date/source/deleted_at/シフト列)に保存、data 経由のみ
- [ ] AC2 開始>終了で保存されず Voice 文言
- [ ] AC3 削除で `deleted_at` + 一定時間 Undo、読み取りは deleted_at IS NULL 強制
- [ ] AC4 オフライン書き込みキュー、オンライン復帰でフラッシュ

### Story 1.5 月 / 週 / リストのカレンダー表示
- [ ] AC1 月(7列・今日リング)/ 週(時刻軸・現在時刻ライン)/ リスト(日付昇順)、表示オンのカレンダー予定が全ビューで見える
- [ ] AC2 チップは色バー + 時刻 + タイトル、色だけで所属を表さない
- [ ] AC3 横スワイプで前後移動 / 週スロットタップで時刻プリセット追加、単一カラム、PC 幅で月広く

### Story 1.6 PWA インストールとオフライン閲覧
- [ ] AC1 マニフェスト・アイコン・スプラッシュ、インストール可
- [ ] AC2 オフラインでローカルデータ表示、取り込み系 UI に「オフライン」

### Story 1.7 データの JSON エクスポート
- [ ] AC1 ローカルのカレンダー・予定を含む JSON
- [ ] AC2 取り込んだ外部予定は含まない
- [ ] AC3 お気に入りシフトのテンプレを含む(4.1 で完成)

---

## Epic 2 — 優先度をつけて、大事な予定を先に見る

### Story 2.1 カレンダーの優先度設定(ドラッグ + キーボード)
- [ ] AC1 ドラッグで並べ替え → `calendars.priority` 更新、unique + RPC で一意
- [ ] AC2 キーボード「上へ/下へ」で同じ並べ替え、SR が「優先度 N/M」を読む
- [ ] AC3 新規カレンダー/取り込みは既定で最下位

### Story 2.2 優先度ユーティリティと一覧・リストの並び
- [ ] AC1 一覧・リストが優先度順、同順位内は開始時刻順、core の純関数を通る
- [ ] AC2 core 優先度ユーティリティに単体テスト
- [ ] AC3 優先度変更が再読み込みなしで反映

### Story 2.3 重なった予定の視覚的な優先表示(週・月)
- [ ] AC1 週の重なりで優先度高が左端、並べきれない場合は前面(← v1 deferred: 列キャップ未実装)
- [ ] AC2 月で入りきらない分は優先度順 + 「他 N 件」、開くと優先度順一覧

### Story 2.4 代表予定の選抜ロジック(packages/core)
- [ ] AC1 `selectFeaturedEvents` は副作用なし、`now` は引数、正規入力型を core が定義
- [ ] AC2 5規則どおりの順序付き結果、0件は空
- [ ] AC3 5規則それぞれに単体テスト

### Story 2.5 ホームのコンパクトビュー
- [ ] AC1 compact-card が選抜結果を低密度・優先度順、各行にカレンダー名・色・開始時刻、件数 1〜3 設定
- [ ] AC2 代表予定タップで該当日へ遷移
- [ ] AC3 予定なしで「この後の予定はありません」1行、感嘆符なし
- [ ] AC4 SR 読み上げ順が優先度順

---

## Epic 4 — シフトを楽に入れて、今月の給料見込みを見る

### Story 4.1 お気に入りシフトの登録
- [ ] AC1 テンプレ CRUD が `shift_templates` + RLS、複数可、chip はピル形「名前 + 時間帯」
- [ ] AC2 負値・休憩 ≥ 実働 でバリデーション
- [ ] AC3 JSON エクスポートにテンプレが含まれる

### Story 4.2 ワンタップ・シフト入力(quick-shift-sheet)
- [ ] AC1 月/週で日付タップ → quick-shift-sheet、テンプレ横スクロール
- [ ] AC2 チップ1タップでシフト作成(events にシフト属性 + シフト用カレンダー所属)、シート閉じ、3タップ以内
- [ ] AC3 連続複数日選択で全日にシフト
- [ ] AC4 テンプレ未登録で日付タップ → 案内 + 作成ボタン
- [ ] AC5 汎用予定編集で開始/終了/メモを変えてもシフト属性は不変

### Story 4.3 実働時間の計算(packages/core pay-calc)
- [ ] AC1 実働 = 終了 − 開始 − 休憩、日またぎは翌日通算・分割しない
- [ ] AC2 通常・休憩控除・日またぎの単体テスト

### Story 4.4 当月の給料見込みとトップ画面表示(pay-card)
- [ ] AC1 pay-card が当月見込み(実働 × 時給 の暦月合計、時給差合算)を amount 書式、都度計算
- [ ] AC2 前月/翌月矢印で対象月切替 / シフト変更で即時再計算
- [ ] AC3 シフト0で「¥0」+「今月のシフトはまだありません」
- [ ] AC4 タップで内訳、割増・締め日・勤務先別の設定は出さない

---

## 検証ログ(2026-09-11、localhost:5173、una.catena.della.morte でログイン中)

### Epic 1

| Story | AC | 判定 | 根拠 |
|---|---|---|---|
| 1.1 | AC1 | 🖥️ | `supabase start` は Docker 無しで実行不可(既知)。モノレポ構成 + `packages/core` / `src/features` / `src/data` / `src/app` / `supabase/` は `npm run build` green で確認 |
| 1.1 | AC2 | ✅ | 下タブ「ホーム / カレンダー / 設定」の3画面、ドロワー/ハンバーガーなし(実機)。`navigation "メインナビゲーション"` |
| 1.1 | AC3 | ✅ | 設定「テーマ」= 端末に合わせる / ライト / ダーク。ライトに切替 → アプリ全体が light パレット(白地・青アクセント #2563EB)に。ダークへ戻ることも確認 |
| 1.2 | AC1 | ✅ | 設定「アカウント」= ログイン中 + メール + ログアウト。`calendars`/`events`/`shift_templates` の migration に各 4 RLS ポリシー(`user_id = auth.uid()`)。Epic 3 でも検証済み |
| 1.2 | AC2 | ✅ | 未ログイン(guest)でもローカル CRUD 可 = `HomeScreen` の `enabled = state==='guest' \|\| 'authenticated'`。匿名サインイン方式。実機はログイン状態で確認 |
| 1.2 | AC3 | ✅ | `ConnectionsSection.tsx:120` `state==='guest'` → 「接続にはログインが必要です。」+「ログインして接続」→ `navigate('/auth')`。Epic 3 Phase C でも実機確認済み(匿名は Google 接続不可) |
| 1.3 | AC1 | ✅ | 「検証用カレンダー」作成 → 名前「検証用２」+ 色変更 → 削除(Undo バー)。一覧に source「ローカル」明示(実機) |
| 1.3 | AC2 | ✅ | シフト用カレンダー(「シフト」`シフト用`)は編集シートに名前・色はあるが**削除ボタンなし**。ローカルカレンダーには「このカレンダーを削除」あり(実機) |
| 1.3 | AC3 | ⚠️ | **初回・データなしの「カレンダーを接続 / 予定を追加」2入口が未実装**。`HomeScreen` は空でも `CompactCard`(「この後の予定はありません」)+ `PayCard` のみ。シフト用カレンダーが常在するため「カレンダー0」は正常運用で到達不能・実害は小さいが、UX-DR13「初回・データなし」の状態を1つ落としている(→ G2) |
| 1.4 | AC1 | ✅ | 「検証イベントA」作成 → 月/週/リストに表示、data-access 経由のみ(UI から supabase-js 直呼びなし = `import type { Session }` のみ)、`Result` 返し |
| 1.4 | AC2 | ✅ | 終了(9/9)を開始(9/11)より前にして保存 → 保存されず「終了は開始より後にしてください」(実機) |
| 1.4 | AC3 | ✅ | 削除 → Undo バー「「…」を削除しました / 取り消す」(events / calendars / shift-templates すべて)。`events.ts` の読み取りは `.is('deleted_at', null)`、`undoDelete` → `restoreEvent`(単体テスト `remove で消え、Undo で戻る`)。※共通ヘルパではなく各クエリ個別指定(→ G6) |
| 1.4 | AC4 | ✅ | `navigator.onLine=false` + offline イベント発火 → 「オフライン ・ 変更は接続後に送信されます」バナー。カレンダーデータは IndexedDB(`calendar-app@1`)キャッシュから表示継続。書き込みキュー(outbox)は 165 tests |
| 1.5 | AC1 | ✅ | 月 = 7列グリッド + 今日(11)にアクセントのリング。週 = 1日タイムライン + 時刻軸 + **現在時刻ライン(青の横線)**。リスト = 日付昇順。表示オンのカレンダー予定が全ビューで表示(実機) |
| 1.5 | AC2 | ✅ | チップ = カレンダー色バー + 時刻 + タイトル(+ リストは右にカレンダー名)。色だけで所属を表さない(実機) |
| 1.5 | AC3 | ⚠️ | 週の空きスロットタップ → タップ時刻をプリセットした「予定を追加」(実機 ✅)。**横スワイプ日付移動は deferred**(`deferred-work.md` spec-1-5)、**PC 幅で月ビューを広く(UX-DR17)は未実装**(`deferred-work.md` spec-1-1、`max-w-2xl` 固定)(→ G3) |
| 1.6 | AC1 | ✅ | **2026-09-12 `npm run build && npm run preview` で実機確認済み**。`manifest.webmanifest` 配信(name「カレンダーアプリ(仮)」/ display:standalone / theme_color #2563EB / icons 192・512・512-maskable)、SW 登録・`active:true`、`workbox-precache-v2` に10エントリ。ページ下部に「オフラインでも使えます」通知(`PwaUpdatePrompt`)表示。dev サーバーでは devOptions 未有効のため出ない(仕様どおり) |
| 1.6 | AC2 | ✅ | 上記の offline バナー(実機)+ IndexedDB キャッシュ存在。取り込み系 UI のオフライン表示は Epic 3 スコープ |
| 1.7 | AC1 | ✅ | `buildExportBundle`(`src/data/export.ts`): `calendars`(local)+ `events`(local かつ書き出すカレンダー所属)。ファイル名 `calendar-app-export-YYYY-MM-DD.json`。単体テストあり。※実 DL クリックは未実施(標準 `<a download>`、Ryo が1回押して確認推奨) |
| 1.7 | AC2 | ✅ | `.filter((c) => c.source === 'local')` / `.filter((e) => e.source === 'local' && localIds.has(e.calendarId))` で外部を明示除外(コメントも明記) |
| 1.7 | AC3 | ✅ | `shiftTemplates` を含む、`schemaVersion: 2`(4.1 で追加済み) |

### Epic 2

| Story | AC | 判定 | 根拠 |
|---|---|---|---|
| 2.1 | AC1 | ⚠️ | ドラッグ = HTML5 DnD 実装済み(全 `<li>` draggable)。実機検証は ▲▼ で実施(スムーズなタッチドラッグ / `@dnd-kit` は deferred)。`reorder_calendars` RPC + `(user_id, priority)` 部分ユニーク索引 |
| 2.1 | AC2 | ✅ | 「「シフト」を上へ」で並べ替え → ランク番号(1/2)即更新。SR: ドラッグハンドルに `sr-only`「優先度 1/2、」+ 「ローカル」、▲▼ ボタンに「「…」を上へ/下へ」(実機 + a11y ツリー) |
| 2.1 | AC3 | ✅ | 「検証用カレンダー」作成 → **position 3(最下位)** で追加(実機) |
| 2.2 | AC1 | ✅ | リストビュー 9/11: 検証イベントA(cal「2」pri1, 4:15)→ 「3 3」(cal「2」9:00)→ 「終日 2 1」(cal「2」all-day)→ 「1 1」(シフト pri2, 9:00)。優先度順・同順位内は時刻順・終日は時刻付きの後(実機) |
| 2.2 | AC2 | ✅ | `packages/core/src/priority.test.ts`(8 tests)、`compareEventsForList` |
| 2.2 | AC3 | ✅ | カレンダー ▲▼ でランク即再描画。`ListView` は `priorityOf` を `useMemo` deps に持ち、共有 state 変化で再ソート(再読み込みなし) |
| 2.3 | AC1 | ✅ | 週ビュー 9/11 の 9:00: 「3 3」(cal「2」pri1)が**左**、「1 1」(シフト pri2)が**右**の並列タイル(実機)。※列キャップ + z-index「前面」は deferred(→ G5) |
| 2.3 | AC2 | ✅ | 月ビュー 9/11 に4件目を追加 → セルが3チップ + **「他 1 件」**。「他 N 件」タップ → その日のリストビュー(優先度順)(実機) |
| 2.4 | AC1 | ✅ | `selectFeaturedEvents(events, priorityOf, now, limit)`: `priority` 以外 import なし、`now` は引数、`FeaturableEvent`(camelCase / UTC ISO)を core が定義(コード) |
| 2.4 | AC2 | ✅ | scope フィルタ(now以降/進行中)→ `compareEventsForList` ソート → `slice(0, limit)`、`limit<=0`/0件は `[]`(コード) |
| 2.4 | AC3 | ✅ | `packages/core/src/featured.test.ts`(15 tests) |
| 2.5 | AC1 | ✅ | ホーム `compact-card`: 「9:00 / 3 3 / 2」「終日 / 2 1 / 2」= 開始時刻 + タイトル + カレンダー名、左に色バー、低密度・優先度順。設定「ホームに出す予定の数」= 1/2/3(実機) |
| 2.5 | AC2 | ✅ | compact 行タップ → `/calendar?date=2026-09-11`(実機) |
| 2.5 | AC3 | ✅ | `CompactCard.tsx:21`「この後の予定はありません」(感嘆符なし) |
| 2.5 | AC4 | ✅ | a11y ツリー: `list "この後の予定"` の `listitem` が DOM 順 = 優先度順 = SR 読み上げ順 |

### Epic 4

| Story | AC | 判定 | 根拠 |
|---|---|---|---|
| 4.1 | AC1 | ✅ | 「バイトA」テンプレ作成(名前/開始/終了/休憩60/時給1100/色)→ pill chip「● バイトA 09:00–18:00」。編集(全項目)・削除(Undo バー)確認(実機) |
| 4.1 | AC2 | ✅ | 休憩 600(分)+ 09:00–18:00(実働 540分)で保存 → 「休憩は0以上で、実働時間より短くしてください」(実機)。負値等は 6 messageKey + 4.1 tests |
| 4.1 | AC3 | ✅ | `export.ts` に `shiftTemplates` + `schemaVersion: 2`(コード) |
| 4.2 | AC1 | ✅ | 9/15 タップ → quick-shift-sheet:「この日から [−] 1 日 [+]」+「● バイトA 09:00–18:00」チップ横並び +「シフト以外の予定を追加」(実機) |
| 4.2 | AC2 | ✅ | チップ1タップ → シフト作成 + シート閉じ。作成された予定は**カレンダー「シフト」所属**、汎用編集シートで開く(実機)。タップ数: 日タップ + チップタップ = 2 |
| 4.2 | AC3 | ✅ | ステッパを「3 日」→ チップタップ → 9/15・9/16・9/17 の3日すべてにシフト作成(実機) |
| 4.2 | AC4 | ✅ | テンプレ0件の状態で日タップ → 「よく使うシフトを登録すると1タップで入れられます。」+「+ お気に入りシフトを作る」+「シフト以外の予定を追加」(実機) |
| 4.2 | AC5 | ✅ | シフト実体を汎用編集シートで開くと 休憩分・時給・勤務先ラベルの**フィールドが存在しない**(タイトル/カレンダー/終日/開始/終了/メモのみ)= AD-8 を構造で担保(実機) |
| 4.3 | AC1 | ✅ | pay-detail: 9/15 09:00–18:00 → **8.0h × ¥1,100 = ¥8,800**(実働 = 540 − 60休憩 = 480分 = 8h)(実機)。`workedMinutes` + code |
| 4.3 | AC2 | ✅ | `packages/core/src/pay.test.ts`(13 tests、通常・休憩控除・日またぎ) |
| 4.4 | AC1 | ✅ | pay-card「9月の給料見込み ¥26,400 / 9月 ・ 5件のシフト」= 8,800 × 3。都度計算(`useMemo`)。tabular-nums + カンマ区切り(実機) |
| 4.4 | AC2 | ✅ | シフト3件追加 → ¥0 → ¥26,400 に即時再計算。pay-card ‹ › で 10月 = ¥0 に切替(実機) |
| 4.4 | AC3 | ✅ | 10月(シフトなし)= 「¥0」+「10月のシフトはまだありません」(実機) |
| 4.4 | AC4 | ✅ | pay-card タップ → 内訳シート「9月の給料見込み」= 日付 + 時間帯 + `Nh × ¥W = ¥小計` の行 + 「合計 ¥26,400」。割増・締め日・勤務先別の設定項目なし(実機) |

### 検証で見つかった逸脱・ギャップ(retro へ)

- **G1**(Epic 1 / 1.1・1.6・1.4): ローカル DB / Edge Function / PWA の統合テストが無い(Docker 無し・dev で SW 無効)。Epic 3 の F5 と同根。実機検証で代替しているが構造的に脆い。
- **G2**(Epic 1 / 1.3 AC3): 初回・データなしの「カレンダーを接続 / 予定を追加」2入口が未実装。UX-DR13 の状態を1つ落としている。実害小(シフト用カレンダー常在)。
- **G3**(Epic 1 / 1.5 AC3・UX-DR17): 月/週の横スワイプ日付移動、PC 幅で月ビューを広く ── どちらも `deferred-work.md` 記載済み。個人スマホ利用で実害小。
- **G4**(Epic 1 / 1.6): ~~PWA の実機インストール/オフライン SW は `npm run build && preview` でしか確認できない~~ **→ 2026-09-12 解消**。manifest 配信・SW 登録・precache 10エントリ・オフライン通知バナーを `npm run preview` で実機確認済み。
- **G5**(Epic 2 / 2.3 AC1): 週の重なり列キャップ + z-index「前面」表示(FR-7「並べきれない場合」)未実装。`deferred-work.md` 記載済み。個人カレンダーで4件同時重なりは稀。
- **G6**(Epic 1 / 1.4 AC3): `deleted_at IS NULL` が共通ヘルパではなく各読み取りクエリで個別指定。invariant は保たれるが規約(共通ヘルパ)と乖離。
- **minor**(Epic 4 / 4.1): 無効入力 → 修正 → 成功後に、画面レベルの stale validation error バナーが残る(「閉じる」で消える)。

### 総合

**16 ストーリーすべて done 可**。ブロッカーなし。G1〜G6 + minor は「終わったエピックの宿題」として各 retro に記録し追跡する(G2 以外はすべて既存の `deferred-work.md` に記載済み or 既知の環境制約)。実機で全 story の主要フローを end-to-end で通し、テストが薄い純ロジック外(offline・優先度並び・quick-shift・給料計算)を実際に動かして確認した。テスト green(435)+ typecheck / lint / build clean。
